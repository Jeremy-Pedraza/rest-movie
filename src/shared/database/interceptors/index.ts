// src/shared/database/interceptors/index.ts

/**
 * @fileoverview Re-export de interceptors de tenant
 * @module shared/database/interceptors
 *
 * Los interceptors de tenant están en src/interceptors/ (globales)
 * Este archivo re-exporta para acceso conveniente
 */

export { TenantInterceptor } from '@interceptors/tenant.interceptor';
